import 'package:flutter/material.dart';

class BaixaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Baixa'),
      ),
      body: Center(
        child: Text('Tela de Baixa'),
      ),
    );
  }
}
